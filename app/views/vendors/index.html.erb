<h1>Here are all the vendors in MARVL sorted by category</h1>

<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %></li>
      <ul>
        <% category.vendors.each do |vendor| %>
        <li><%= link_to vendor.name, vendor %> | <%= vendor.reviews.count %> CPA member reviews | Average rating: <%= vendor.avg_rating %></li>
        <% end %>
      </ul>
  <% end %>
</ul>

<h1>Know an outstanding vendor who should be on this list?  Add them now!</h1>

<%= form_for @vendor do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :street_address %>
  <%= f.text_field :street_address %><br>
  <%= f.label :city %>
  <%= f.text_field :city %><br>
  <%= f.label :state %>
  <%= f.text_field :state %><br>
  <%= f.label :zip_code %>
  <%= f.text_field :zip_code %><br>
  <%= f.label :phone %>
  <%= f.text_field :phone %><br>
  <%= f.label :email %>
  <%= f.text_field :email %><br>
  <%= f.label :website %>
  <%= f.text_field :website %><br>

  <%= f.submit "Submit Vendor" %> 
<% end %>