<h1>Here are all the vendors in the <%= @category.name.upcase %> category.</h1>

<% if @category.vendors.count > 0 %>
  <ul>
    <% @category.vendors.each do |vendor| %>
      <li><%= link_to vendor.name, vendor %> | <%= vendor.reviews.count %> CPA member reviews | Average rating: <%= vendor.avg_rating %></li>
    <% end %>
  </ul>
<% end %>

<h2>Recent reviews for <%= @category.name.upcase %>.</h2>
  <div>
    <% @recent_reviews.each do |review| %>
      <div>
        <%= link_to review.title, vendor_review_path(review.vendor_id, review.id) %>
        <p><%= review.content %></p>
      </div>
    <% end %> 
  </div>