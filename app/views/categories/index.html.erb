<h1>Here are all the current categories covered in MARVL</h1>

  <div>
    <% @categories.each do |category| %>
      <div>
      	<%= link_to category.name, category_path(category) %>

      </div>
    <% end %> 
  </div>