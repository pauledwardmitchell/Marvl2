<h2> View/Manage Users </h2>



<table>
  <tr>
    <th>
    <th>first
    <th>last
    <th>email
    <th>member?
    <th>expiration
    <th>admin?
  </tr>
<% @users.each do |user| %>
  <tr>
    <td><%= link_to '👤', "/users/#{user.id}" %>
    <td><%= user.first_name %>
    <td><%= user.last_name %>
    <td><%= user.email %>
    <td><%= 'yes' if user.member? %>
      <span> <%= link_to '📝',
    <td><%= user.membership_expiration %>📝
    <td><%= 'yes' if user.is_admin? %>📝

<% end %>
</table>

